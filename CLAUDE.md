# AI 行为准则 + 信息架构
> Layer 1 — AI 行为铁律 + 文档索引。每次会话自动加载。

---

## 文档索引

| 动作 | 关联文档 |
|------|---------|
| 写后端代码 | `src/api/CLAUDE.md` + `docs/项目架构宪法.md` §标准形状 |
| 写插件前端 | `src/chrome-ext/CLAUDE.md` |
| 改架构 | `docs/项目架构宪法.md` 全文 |
| 排查代码 / 定位文件 | `/file-map` skill |
| 不确定 | `docs/README.md` 索引 |

> ⚠️ **提醒（100% 弹出）：** 匹配到上述文档时，先问用户：「`XX.md` 可能已过时。要我查文档还是你直接告诉我规则？」再继续。
>
> ⚠️ **新增类/模块/配置/依赖 → 暂停确认：**「这个抽象真的需要吗？会不会过度设计？」

### 文档规则
1. `docs/README.md` 为唯一文档入口
2. 已有文档不重复创建
3. 新建文档 → 更新 `docs/README.md` 索引

---

## 项目全景

```
src/api/       — FastAPI 后端（routes/services/adapters/repositories/domain/utils）
src/chrome-ext/ — Chrome 插件（MV3，side panel + service worker）
docs/web/      — 原生 HTML/CSS/JS 前端（无框架，i18n + samples）
docs/          — 项目文档（README.md 入口）
db/            — MySQL schema
```

> 详细文件地图见 `/file-map` skill。

---

## 铁律

### 一、手术式修改
- 只改必须改的，不改进相邻代码、注释、格式
- 匹配现有风格
- 无关的死代码 → 指出，不删
- 因你的改动产生的孤立代码 → 删
- **铁律一优先于铁律三：简化非本次改动的现有代码前，必须先向用户确认**

### 二、先读后写
- 改文件前先读它的当前内容
- 新增功能前先读同层现有文件结构
- 新增 API 端点前 grep 已有路由
- **禁止凭训练数据推测项目结构**

> **有替换旧东西 或 涉及 ≥2 文件 → 先运行 `/pre-flight` 再做改动。**

### 三、简单至上 + 过度设计判定（客观清单）
满足任意一条 = 过度设计，需简化：
```
□ 新增了需求未提及的配置项
□ 为只调用一次的逻辑创建了独立类/模块
□ 函数参数超过 5 个且部分参数当前未被使用
□ 新增的抽象层只有一个实现
□ 引入了当前需求不需要的第三方依赖
```

### 四、修改联动检查
同一个值出现在 ≥3 个文件中 → 先向用户说明，等确认后再抽取。
- 改函数签名 → grep 所有调用方

**每次代码改动完成后，执行以下 5 项检查（不允许跳过）：**
```
1. 改过的名字 → grep 全项目（含 CLAUDE.md）确认无残留旧名
2. 改过的常量 → grep 前端是否硬编码旧值
3. 改过的文件 → 确认无函数体 import、无死引用
4. 新增/删除文件 → 确认注册/解注册
5. 新增/重命名/删除文件 → 更新 `/file-map` skill 文件地图
```

### 五、异步竞态防御
1. 创建新的 setInterval/Task 前先清理旧的
2. 达到终止条件时，先杀自己再通知外部
3. 全局可变容器（dict/list/queue）必须有上限和清理机制

### 六、修改边界
| 类型 | 能否修改 |
|------|---------|
| `CLAUDE.md` / `docs/项目架构宪法.md` | ❌ 需明确授权 |
| 项目源代码 | ✅ |
| `.claude/` / `.vscode/` | ❌ |
| 系统级配置 | ❌ |

讨论不算授权——用户说"执行""改"才算。

### 七、违宪处理
对照 `docs/违宪自检速查表.md` 发现违宪后，按三级处理：

| 级别 | 判定 | 处理 |
|------|------|------|
| 一级 | 越层调用、黑名单技术栈 | 直接拒绝 |
| 二级 | 未校验身份、新建文件未注册 | 24h 修复 |
| 三级 | 函数超 50 行、命名不一致 | 提醒修正 |

### 八、多步任务中途检查点
超过 2 步 → 先列计划，每 1-2 步停下汇报，等确认后继续。

### 九、翻译归属规则（铁律）

**任何用户可见文字必须归属于以下三层之一，禁止在 Python/JS 代码中硬编码展示文案。**

| 层 | 判断标准 | 文件 |
|----|---------|------|
| 🖥️ 前端i18n | 每种商品一样 | `docs/web/lang/i18n/{lang}.json` |
| 📖 字典查表 | 中文由我们产出、可穷举 | `src/api/domain/data/glossary.json` |
| 🤖 Qwen翻译 | 中文来自1688、不可穷举 | translator.py，无固定配置 |
| ➖ 不翻译 | 数字/URL/图片 | 无 |

**改 `term_glossary.json` 中文原文时，必须同步检查 en/vi/th/id 四语言翻译。不同步 → 删旧值，前端降级显示中文。**

---

## 代码约定
- 注释写 WHY 不写 WHAT
- 删函数/类/变量前 grep 全项目确认零引用
- 扩展现有文件优于新建

## 输出格式
- 不确定时标置信度：**确定** / **推测** / **存疑**
- 涉及跨文件架构判断且清单无法覆盖 → 说明"需人工判断"，不给看似自信的架构结论

## 文档创建规则
- 创建新文档 → 更新 `docs/README.md` 索引
