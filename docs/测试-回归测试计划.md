# 回归测试计划

> 2 轮审计收尾后的运行时验证。静态审计抓「代码层错误」，本计划抓「运行时行为」——把第一轮 G/H/N 里「仅文档标已修、无独立验证」的弱证据项，升级成「跑过确认」。
> 决策日期：2026-08-17

## 背景

- 第一轮审计（[技术-完善修复方案.md](技术-完善修复方案.md)）：39 项隐患（G1-G12 / H1-H5 / N1-N22），36 已修 + N11 已核 + 3 跳过
- 第二轮审计（audit-matrix 70 子项）：修 S1.6 / S1.1 / DOC8.1 / CS7.2 / CS7.4 / R3.3 / RS4.1 / CS7.10 / DOC8.2-8.4
- 静态审计已扫完，剩下只能靠真实跑一遍验证运行时行为

## 前置条件

- 后端运行 + 真 `.env`（APIFY_TOKEN / QWEN_API_KEY / GOOGLE_CLIENT_ID / DB 均配好）

## 一、自动化

| # | 内容 | 命令 | 覆盖审计项 |
|---|---|---|---|
| A1 | 单元测试 | `pytest tests/` | G1-G4 / N2 / N6 / S1.6 |
| A2 | 端到端 13 节点 | `python tests/e2e_flow.py` | 流水线全节点 + G/H 隐患假设 |

## 二、手动（人操作 / 人眼判断）

### 核心（必做）

| # | 内容 | 为什么手动 | 覆盖审计项 |
|---|---|---|---|
| M1 | 插件 Google 登录 → token 落 storage → 进分析页 | 真实账号授权弹窗，脚本模拟不了 | N1 / N18 |
| M2 | 真实 1688 链接分析，切 zh/en/vi/th 看判词 + 翻译 | Qwen 翻译质量要人眼看 | R3.2 / N2 / D6.2 |
| M3 | 历史列表 + 收藏 + 供应商等级点颜色 + 商品对比 | 前端渲染人眼看 | H1 / H2 / H5 |
| M4 | DevTools 查：console 无 token、无中文乱码、无 XSS | 安全项只能 DevTools 看 | S1.1 / N1 / N3 |

### 边缘（有条件做）

| # | 内容 | 为什么手动 | 覆盖审计项 |
|---|---|---|---|
| M5 | Qwen 失败降级（临时改错 QWEN_API_KEY）→ 看是否出模板判词 + summaryLine 非空 | 要主动触发降级 | R3.2 / F5.5 |
| M6 | 同链接连续失败 → 出现「明天再试」提示 | 要触发 _fail_count 阈值（≥3） | G6 / CS7.2 |
| M7 | Apify 配额耗尽 → 自动切下一个 token | 需 2 个真 token 且一个真耗尽 | G6 / N11 |

## 执行顺序

1. A1 自动化兜底（先跑，绿了再往下）
2. A2 端到端（真实抓取 + 落库）
3. M1-M4 核心手动（人工过一遍）
4. M5-M7 边缘手动（能触发就补）
